num = [1, -5, 3, -8, 5, 0, 7]

sindx = 0

eindx = len(num)-1

while sindx < eindx:

    if num[sindx] < 0:

        sindx += 1

    elif num[eindx] > 0:

        eindx -=1

    else:

        num[sindx], num[eindx] = num[eindx], num[sindx]

print(num)
