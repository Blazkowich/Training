words = input()
vowels = ['a', 'e', 'i', 'o', 'u']
vows = 0
cons = 0
for word in words:
    if word in vowels:
        vows += 1
    elif word != ' ':
        cons += 1
print("Vowels : ", vows)
print("Cons : ", cons)
