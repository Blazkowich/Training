numberList = [1, 2, -3, -1, -2, 3]
listLength = len(numberList)
startingIterator = 0
endingIterator = 0
while startingIterator < listLength:
    if numberList[startingIterator] > 0:
        startingIterator += 1
    else:
        for indexNumber in range(startingIterator, endingIterator, -1):
            numberList[indexNumber], numberList[indexNumber - 1] = numberList[indexNumber - 1], numberList[indexNumber]
        startingIterator += 1
        endingIterator += 1
print(numberList)
